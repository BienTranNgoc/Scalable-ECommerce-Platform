version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    env_file:
      - .env
    volumes:
      - .:/app
    ports:
      - "${APP_PORT}:${APP_PORT}"
